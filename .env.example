## Database
# Single connection URL (preferred)
DB_URL=postgresql://postgres:password@localhost:5432/postgres

## CORS
# Comma-separated list of allowed origins. Use * to allow all.
CORS_ORIGINS=*

## Celery + Redis
# Redis URLs for Celery broker and result backend
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
# How long workers block on BRPOP before retrying (seconds)
REDIS_BRPOP_TIMEOUT=60
# Redis health check PING interval (seconds)
REDIS_HEALTH_CHECK_INTERVAL=60
# Limit broker connection pool size per worker process
BROKER_POOL_LIMIT=2
# Worker heartbeat/event interval (seconds)
WORKER_HEARTBEAT=60
# Disable Celery runtime events and remote control to reduce PUB/SUB traffic
WORKER_SEND_TASK_EVENTS=false
TASK_SEND_SENT_EVENT=false
WORKER_ENABLE_REMOTE_CONTROL=false

## Mapbox (kept)
MAPBOX_USERNAME=mynameis123
MAPBOX_API_ACCESS_TOKEN=myapitoken456
MAPBOX_TILESET_ID=mytilesetid789
MAPBOX_UPDATE_TILESET_ENDPOINT=https://api.mapbox.com/tilesets/v1

## Pagination
# Default items per page for list endpoints (min 1, max respected by MAX_PAGE_SIZE)
DEFAULT_PAGE_SIZE=100
# Soft upper bound for page size to prevent unbounded queries
MAX_PAGE_SIZE=1000
